import { ArgTypes } from '@storybook/blocks';
import { ContactPointSelector } from './ContactPointSelector';

# ContactPointSelector

A component for selecting contact points in Metrics Dashboard. Only works with the built-in Metrics Dashboard Alertmanager.
